#include <iostream>
#include <string>
#include <vector>
#include <sstream>

using namespace std;
class Utilities{
public:
    static std::vector<std::string> tokenize(std::string const &str, const char delim);
};